{
  "name": "@actions-internal/spell-checker-filename",
  "version": "0.0.0",
  "main": "src/main.ts",
  "license": "MIT",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@types/node": "^25.0.9",
    "@types/nspell": "^2.1.6",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@actions/core": "^2.0.2",
    "@actions/github": "^7.0.0",
    "async-mutex": "^0.5.0",
    "nspell": "^2.1.5"
  },
  "scripts": {
    "prebuild": "shx rm -rf dist/*",
    "build": "esbuild ./src/main.ts --bundle --outfile=dist/index.js --platform=node --format=esm --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "test": "node --version && node --test '**/*.test.*'",
    "lint:biome": "biome check"
  }
}
