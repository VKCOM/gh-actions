{
  "type": "module",
  "name": "@actions-internal/bump-vkui-floating-ui-version",
  "version": "0.0.0",
  "main": "src/main.ts",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/node": "^25.2.0",
    "@types/semver": "^7.7.1",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/exec": "^3.0.0",
    "@actions/github": "^9.0.0",
    "semver": "^7.7.3"
  },
  "scripts": {
    "prebuild": "shx rm -rf dist/*",
    "build": "esbuild ./src/main.ts --bundle --outfile=dist/index.js --platform=node --packages=bundle --format=esm --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "test": "node --version && node --test '**/*.test.*'",
    "lint:biome": "biome check"
  }
}
