{
  "type": "module",
  "name": "@actions-internal/size-upload",
  "version": "0.0.0",
  "main": "src/main.ts",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/mime-types": "^3.0.1",
    "@types/node": "^25.0.9",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@actions/core": "^2.0.2",
    "@actions/exec": "^2.0.0",
    "@aws-sdk/client-s3": "3.726.1",
    "mime-types": "^3.0.2"
  },
  "scripts": {
    "prebuild": "shx rm -rf dist/*",
    "build": "esbuild ./src/main.ts --bundle --outfile=dist/index.js --platform=node --packages=bundle --format=esm --banner:js=\"import { createRequire } from 'module'; const require = createRequire(import.meta.url);\"",
    "test": "node --version && node --test '**/*.test.*'",
    "lint:biome": "biome check"
  }
}
