{
  "type": "module",
  "name": "@actions-internal/push-screenshots",
  "version": "0.0.0",
  "main": "src/main.ts",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/node": "^25.0.9",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@actions/core": "^2.0.2",
    "@actions/exec": "^2.0.0"
  },
  "scripts": {
    "prebuild": "shx rm -rf dist/*",
    "build": "esbuild ./src/main.ts --bundle --outfile=dist/index.js --platform=node --format=esm --packages=bundle",
    "test": "node --version && node --test '**/*.test.*'",
    "lint:biome": "biome check"
  }
}
